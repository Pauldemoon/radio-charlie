{
  "name": "sillage-fm",
  "version": "1.0.0",
  "private": true,
  "description": "Application web d'emission musicale par voix off.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check server-functions/plan.js && node --check server-functions/speak.js && node --check server-functions/status.js && node --check test.js",
    "test": "node test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
