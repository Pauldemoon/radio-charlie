{
  "name": "sillage-fm",
  "version": "1.0.0",
  "private": true,
  "description": "Sillage FM podcast generator",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check server-functions/plan.js && node --check server-functions/speak.js && node --check server-functions/status.js && node --check test.js",
    "test": "node test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
